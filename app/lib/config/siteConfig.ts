export const siteConfig = {
  // Billing / monetization
  billingEnabled: false, // 🔥 soft open (no payments yet)

  // Feature flags
  membershipsEnabled: true,
  marketplaceEnabled: false,
  programsEnabled: true,

  // Access modes
  ownerModeEnabled: true,
};
